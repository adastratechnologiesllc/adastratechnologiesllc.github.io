document.addEventListener('DOMContentLoaded', function() {
  // Initialize the mobile side navigation menu
  var elems = document.querySelectorAll('.sidenav');
  var instances = M.Sidenav.init(elems);
});
